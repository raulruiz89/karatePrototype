name: Karate Tests

on:
  pull_request:
    branches: [ main, develop ]
  push:
    branches: [ main, develop ]
  workflow_dispatch:  # Enables manual trigger
  schedule:
    - cron: '0 0 * * 0'  # Ejecutar cada domingo a medianoche

permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'
          
      - name: Run Tests
        run: mvn clean test
        
      # (removed) Upload Karate Reports via actions/upload-artifact
      # We rely on the pages actions to publish reports directly. Removing this avoids any
      # indirect dependency on deprecated artifact action versions.
          
      # Setup Pages
      - name: Setup Pages
        uses: actions/configure-pages@v4

      # Upload artifact
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v2
        with:
          path: 'target/karate-reports'

      # Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        id: deployment
        if: github.ref == 'refs/heads/main'
        uses: actions/deploy-pages@v3